{
  "name": "kth-azure-app",
  "version": "0.1.0",
  "description": "",
  "main": "app.js",
  "private": true,
  "dependencies": {
    "@kth/basic-html-templates": "^1.0.44",
    "@kth/http-responses": "^1.0.51",
    "@kth/npm-template": "1.0.14",
    "applicationinsights": "^1.8.10",
    "axios": "^0.21.1",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "express-session": "^1.17.1",
    "kth-node-log": "^3.1.5",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^8.4.0",
    "nodemon": "^2.0.7"
  },
  "scripts": {
    "start": "node app.js",
    "start-dev": "LOG_LEVEL=debug APPLICATIONS_API_KEY=AAA-BBB-CCC PORT=3000 LOG_LEVEL=debug ENV_TEST=SECRET_VALUE_ON__MONITOR nodemon app.js",
    "test": "NODE_ENV=development node_modules/mocha/bin/mocha tests/unit-tests/test-*.js",
    "test-integration": "URL_PREFIX=http://localhost:3000/kth-azure-app ENV_TEST=SECRET_VALUE_ON__MONITOR ./tests/integration-tests/basic.sh",
    "test-integration-in-docker": "ID=$(docker build -q .) && FLOTTSBRO_API_KEY=${FLOTTSBRO_API_KEY} LOCAL_IMAGE_ID=$ID docker-compose -f docker-compose-integration-tests.yml up --force-recreate --abort-on-container-exit --always-recreate-deps",
    "test-unit-in-docker": "ID=$(docker build -q .) && LOCAL_IMAGE_ID=$ID docker-compose -f docker-compose-unit-tests.yml up --abort-on-container-exit --always-recreate-deps",
    "clean": "rm -r ./node_modules && rm -f package-lock.json",
    "package-update": "npm run clean & ncu -u && npm install && npm test"
  },
  "engines": {
    "node": "^14.0.0"
  }
}
