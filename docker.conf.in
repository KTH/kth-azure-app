#
# Docker image name, and also used as COMPOSE_PROJECT_NAME when
# running 'manage.sh -up'. This should also normally be the git repo name.
#
# ex: IMAGE_NAME="niskeo"
#
IMAGE_NAME="kth-azure-app"

#
# Human readable image version. Used for tagging the built image.
#
# ex: IMAGE_VERSION="13.37"
#
IMAGE_VERSION="1.0.1"

# Add custom tags to be added to a built image. Separate tags by comma.
# i.e: CUSTOM_TAGS="dev,java,feature-8"
#
# CUSTOM_TAGS="dev,ref,prod"
